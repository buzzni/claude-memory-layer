// Compatibility export for callers that import the historical tools module.
// Canonical tool ownership now lives in the generated registry.
export { tools } from './registry.js';
export type { McpToolProfile } from './registry.js';
